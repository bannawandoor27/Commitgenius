# GitHub Configuration
# Create a personal access token with 'repo' scope at https://github.com/settings/tokens
GITHUB_TOKEN=your_personal_access_token_here

# Optional: Default base branch for PRs (default is 'develop' if not set)
GITHUB_REPO_BASE_BRANCH=main

# Note: Repository information is now automatically detected from your git remote URL
# No need to manually specify owner and repository name
